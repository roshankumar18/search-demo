# Backend API base URL. Leave empty to proxy /api → http://localhost:5050 (see vite.config.ts)
VITE_API_BASE_URL=

# Fallback brand ID when ?shop= is not in the URL
# Also used as the storefront domain for product image enrichment (e.g. kisah.in)
VITE_BRAND_ID=your-brand-id
